[project]
name = "hive-mock-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "hive-hook",
]

[tool.uv.sources]
hive-hook = { git = "https://github.com/icingtea/hive.git", subdirectory = "hive-hook" }
